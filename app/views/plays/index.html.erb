<% if params[:category].present? %>
	<h1><%= params[:category] %> </h1> <br/>
<% end -%>



<% @plays.each do |play| %>
	<% if params[:category] == play.category.name %>
		<h2><%=link_to play.title, play_path(play.id) %></h2> <br/>
	<%elsif params[:category] == nil %>
		<h2><%=link_to play.title, play_path(play.id) %></h2> <br/>	
	<% end %>
<% end %>

