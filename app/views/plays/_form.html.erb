<%= simple_form_for @play do |f| %>
  <%= f.select :category_id, @categories, :prompt => "Select Category"%>
  <%= f.input :title, label: "Play Title" %>
  <%= f.input :director %>
  <%= f.input :description %>
  <%= f.input :play_img, as: :file %>
  <%= f.button :submit %>
<% end %>